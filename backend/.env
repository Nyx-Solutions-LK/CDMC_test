SQLITE_PATH=./data/cdmc.sqlite
SESSION_SECRET=change-this-to-a-random-string
PORT=4000
MAX_AUDIO_UPLOAD_BYTES=15728640
SEED_ADMIN_USERNAME=admin
SEED_ADMIN_EMAIL=admin@cdmcsrilanka.com
# Session cookie requires HTTPS by default when NODE_ENV=production. Leave
# unset locally (defaults to false over plain HTTP). Only set to false in a
# real deployment if you've confirmed TLS is correctly terminated upstream.
# COOKIE_SECURE=true
